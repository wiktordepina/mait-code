"""Rebuild database tool — reconstructs vector DB from markdown observations."""


def main():
    """Rebuild the memory database from markdown source files."""
    # Placeholder: will parse observations/ and reflections/, rebuild SQLite + vector index
    print("rebuild-db: not yet implemented")
